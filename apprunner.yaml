version: 1.0
runtime: nodejs18
build:
  commands:
    - npm ci
    - npm run client:install
    - npm run server:install
    - npm run client:build
run:
  command: node server/index.js
  network:
    port: 8080
env:
  - name: NODE_ENV
    value: production
  - name: PORT
    value: "8080"
